import { HeaderRoot as root } from "./HeaderRoot";
import { HeaderIcon as icon } from "./HeaderIcon";
import { HeaderTitle as title } from "./HedaerTitle";

const Header = {
  Root: root,
  Icon: icon,
  Title: title
}

const HeaderRoot = Header.Root
const HeaderIcon = Header.Icon
const HeaderTitle = Header.Title

export {
  HeaderRoot,
  HeaderIcon,
  HeaderTitle
}